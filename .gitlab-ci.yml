stages:
  - build-and-test-some
  - build-and-test-all
  - benchmarks

include:
  - local: ".gitlab/build-and-test-all.yml"
  - local: ".gitlab/build-and-test-some.yml"

macrobenchmarks:
  stage: benchmarks
  trigger:
    include: ".gitlab/benchmarks.yml"
  rules:
    - if: $NIGHTLY_BENCHMARKS || $CI_COMMIT_REF_NAME == "master" || $CI_COMMIT_REF_NAME =~ /^release\/v/
      when: always
    - when: manual
