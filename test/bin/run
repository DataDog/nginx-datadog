#!/bin/sh

cd "$(dirname "$0")/../"

set -e

if [ "$BASE_IMAGE" = '' ]; then
    . ../nginx-version-info
fi
export BASE_IMAGE

export NGINX_MODULES_PATH=${NGINX_MODULES_PATH:-/usr/lib/nginx/modules}

docker-compose build --parallel
python3 -m unittest "$@"
