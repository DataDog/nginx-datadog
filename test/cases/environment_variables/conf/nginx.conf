error_log stderr;

load_module modules/ngx_http_datadog_module.so;

events {
    worker_connections  1024;
}

http {
    access_log /dev/stdout;
    
    server {
        listen       8080;

        location / {
            # TODO
            return 200 "I am a fish man $datadog_env_DD_TRACE_SAMPLING_RULES";
        }
    }
}
