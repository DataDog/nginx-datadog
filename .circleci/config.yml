version: 2.1

jobs:
  build:
    parameters:
      build-image:
        type: string
      nginx-tag:
        type: string
    docker:
    - image:  << parameters.build-image >>
    resource_class: xlarge
    environment:
      # The `resource_class` implies a memory limit and supposedly a CPU count
      # limit.  However, either there is no actual CPU count limit, or the
      # limit cannot be determined from within the running job.
      # Instead, the number of CPUs on the host machine is all we can see.
      # This means that a command like `make --jobs=$(nproc)` is likely to run
      # out of memory, because while the `resource_class` says it has 2 CPUs,
      # `nproc` will report something like 36.  `make` will then spawn a bunch
      # of processes, and the job will run out of memory.
      # So, here we hard-code the number of CPUs supposedly offered by the
      # `resource_class`.  For information about the `resource_class` values,
      # see:
      # https://circleci.com/docs/2.0/configuration-reference/
      MAKE_JOB_COUNT: 8
    steps:
    - checkout
    - run: printf << parameters.nginx-tag >> >nginx-tag
    - run: make build
    - persist_to_workspace:
        root: .
        paths:
          - .build/libngx_http_datadog_module.so
          - nginx-tag
    - store_artifacts:
        path: .build/libngx_http_datadog_module.so
        destination: ngx_http_datadog_module.so

  test:
    docker:
    # "cimg" means "convenience image."  Helpful images provided by CircleCI.
    # This one contains Python, docker-compose, and a bunch of other things.
    # See https://circleci.com/docs/circleci-images
    - image: cimg/python:3.10.5
    environment:
      # See https://github.com/containers/podman/issues/13889
      DOCKER_BUILDKIT: 0
    steps:
    - checkout
    - attach_workspace:
        at: /tmp/workspace
    - run: mv /tmp/workspace/nginx-tag .
    - run: mv /tmp/workspace/.build/libngx_http_datadog_module.so test/services/nginx/ngx_http_datadog_module.so
    - setup_remote_docker:
        # Cache docker layers somewhere so that we don't have to rebuild test
        # service images every time.
        docker_layer_caching: true
    - run: |
        env | sort
    # Instead of just running the tests, we run the tests and note whether they
    # succeeded.  This way, we can save the verbose output afterward and then
    # fail if the tests didn't succeed.
    # TODO: This sucks, though.  Find a better way.  The output of a failed test
    # runner should be red in the CI interface.
    - run: |
        if test/bin/run --verbose; then
          touch .tests-succeeded
        fi
    - store_artifacts:
        path: test/logs/test.log
        destination: test.log
    - run: |
        if ! [ -f .tests-succeeded ]; then
          cat test/logs/test.log
          printf '\n\nTests failed.  See the test run step for test driver output.  Verbose output is above.\n'
          exit 1
        fi

workflows:
  build-and-test-latest:
    jobs:
    - build:
        name: "build 1.23.1"
        build-image: "datadog/docker-library:nginx-datadog-build-1.23.1"
        nginx-tag: "1.23.1"
    - test:
        name: "test 1.23.1"
        requires:
        - "build 1.23.1"
    - build:
        name: "build 1.23.1-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.23.1-alpine"
        nginx-tag: "1.23.1-alpine"
    - test:
        name: "test 1.23.1-alpine"
        requires:
        - "build 1.23.1-alpine"
  build-and-test-all:
    # TODO: make sure this runs only on release
    jobs:
    - build:
        name: "build 1.23.1"
        build-image: "datadog/docker-library:nginx-datadog-build-1.23.1"
        nginx-tag: "1.23.1"
    - test:
        name: "test 1.23.1"
        requires:
        - "build 1.23.1"
    - build:
        name: "build 1.23.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.23.0-alpine"
        nginx-tag: "1.23.0-alpine"
    - test:
        name: "test 1.23.0-alpine"
        requires:
        - "build 1.23.0-alpine"
    - build:
        name: "build 1.23.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.23.0"
        nginx-tag: "1.23.0"
    - test:
        name: "test 1.23.0"
        requires:
        - "build 1.23.0"
    - build:
        name: "build 1.22.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.22.0-alpine"
        nginx-tag: "1.22.0-alpine"
    - test:
        name: "test 1.22.0-alpine"
        requires:
        - "build 1.22.0-alpine"
    - build:
        name: "build 1.22.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.22.0"
        nginx-tag: "1.22.0"
    - test:
        name: "test 1.22.0"
        requires:
        - "build 1.22.0"
    - build:
        name: "build 1.21.6-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.6-alpine"
        nginx-tag: "1.21.6-alpine"
    - test:
        name: "test 1.21.6-alpine"
        requires:
        - "build 1.21.6-alpine"
    - build:
        name: "build 1.21.6"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.6"
        nginx-tag: "1.21.6"
    - test:
        name: "test 1.21.6"
        requires:
        - "build 1.21.6"
    - build:
        name: "build 1.21.5-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.5-alpine"
        nginx-tag: "1.21.5-alpine"
    - test:
        name: "test 1.21.5-alpine"
        requires:
        - "build 1.21.5-alpine"
    - build:
        name: "build 1.21.5"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.5"
        nginx-tag: "1.21.5"
    - test:
        name: "test 1.21.5"
        requires:
        - "build 1.21.5"
    - build:
        name: "build 1.21.4-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.4-alpine"
        nginx-tag: "1.21.4-alpine"
    - test:
        name: "test 1.21.4-alpine"
        requires:
        - "build 1.21.4-alpine"
    - build:
        name: "build 1.21.4"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.4"
        nginx-tag: "1.21.4"
    - test:
        name: "test 1.21.4"
        requires:
        - "build 1.21.4"
    - build:
        name: "build 1.21.3-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.3-alpine"
        nginx-tag: "1.21.3-alpine"
    - test:
        name: "test 1.21.3-alpine"
        requires:
        - "build 1.21.3-alpine"
    - build:
        name: "build 1.21.3"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.3"
        nginx-tag: "1.21.3"
    - test:
        name: "test 1.21.3"
        requires:
        - "build 1.21.3"
    - build:
        name: "build 1.21.1-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.1-alpine"
        nginx-tag: "1.21.1-alpine"
    - test:
        name: "test 1.21.1-alpine"
        requires:
        - "build 1.21.1-alpine"
    - build:
        name: "build 1.21.1"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.1"
        nginx-tag: "1.21.1"
    - test:
        name: "test 1.21.1"
        requires:
        - "build 1.21.1"
    - build:
        name: "build 1.21.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.0-alpine"
        nginx-tag: "1.21.0-alpine"
    - test:
        name: "test 1.21.0-alpine"
        requires:
        - "build 1.21.0-alpine"
    - build:
        name: "build 1.21.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.21.0"
        nginx-tag: "1.21.0"
    - test:
        name: "test 1.21.0"
        requires:
        - "build 1.21.0"
    - build:
        name: "build 1.20.2-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.20.2-alpine"
        nginx-tag: "1.20.2-alpine"
    - test:
        name: "test 1.20.2-alpine"
        requires:
        - "build 1.20.2-alpine"
    - build:
        name: "build 1.20.2"
        build-image: "datadog/docker-library:nginx-datadog-build-1.20.2"
        nginx-tag: "1.20.2"
    - test:
        name: "test 1.20.2"
        requires:
        - "build 1.20.2"
    - build:
        name: "build 1.20.1-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.20.1-alpine"
        nginx-tag: "1.20.1-alpine"
    - test:
        name: "test 1.20.1-alpine"
        requires:
        - "build 1.20.1-alpine"
    - build:
        name: "build 1.20.1"
        build-image: "datadog/docker-library:nginx-datadog-build-1.20.1"
        nginx-tag: "1.20.1"
    - test:
        name: "test 1.20.1"
        requires:
        - "build 1.20.1"
    - build:
        name: "build 1.20.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.20.0-alpine"
        nginx-tag: "1.20.0-alpine"
    - test:
        name: "test 1.20.0-alpine"
        requires:
        - "build 1.20.0-alpine"
    - build:
        name: "build 1.20.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.20.0"
        nginx-tag: "1.20.0"
    - test:
        name: "test 1.20.0"
        requires:
        - "build 1.20.0"
    - build:
        name: "build 1.19.10-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.10-alpine"
        nginx-tag: "1.19.10-alpine"
    - test:
        name: "test 1.19.10-alpine"
        requires:
        - "build 1.19.10-alpine"
    - build:
        name: "build 1.19.10"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.10"
        nginx-tag: "1.19.10"
    - test:
        name: "test 1.19.10"
        requires:
        - "build 1.19.10"
    - build:
        name: "build 1.19.9-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.9-alpine"
        nginx-tag: "1.19.9-alpine"
    - test:
        name: "test 1.19.9-alpine"
        requires:
        - "build 1.19.9-alpine"
    - build:
        name: "build 1.19.9"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.9"
        nginx-tag: "1.19.9"
    - test:
        name: "test 1.19.9"
        requires:
        - "build 1.19.9"
    - build:
        name: "build 1.19.8-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.8-alpine"
        nginx-tag: "1.19.8-alpine"
    - test:
        name: "test 1.19.8-alpine"
        requires:
        - "build 1.19.8-alpine"
    - build:
        name: "build 1.19.8"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.8"
        nginx-tag: "1.19.8"
    - test:
        name: "test 1.19.8"
        requires:
        - "build 1.19.8"
    - build:
        name: "build 1.19.7-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.7-alpine"
        nginx-tag: "1.19.7-alpine"
    - test:
        name: "test 1.19.7-alpine"
        requires:
        - "build 1.19.7-alpine"
    - build:
        name: "build 1.19.7"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.7"
        nginx-tag: "1.19.7"
    - test:
        name: "test 1.19.7"
        requires:
        - "build 1.19.7"
    - build:
        name: "build 1.19.6-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.6-alpine"
        nginx-tag: "1.19.6-alpine"
    - test:
        name: "test 1.19.6-alpine"
        requires:
        - "build 1.19.6-alpine"
    - build:
        name: "build 1.19.6"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.6"
        nginx-tag: "1.19.6"
    - test:
        name: "test 1.19.6"
        requires:
        - "build 1.19.6"
    - build:
        name: "build 1.19.5-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.5-alpine"
        nginx-tag: "1.19.5-alpine"
    - test:
        name: "test 1.19.5-alpine"
        requires:
        - "build 1.19.5-alpine"
    - build:
        name: "build 1.19.5"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.5"
        nginx-tag: "1.19.5"
    - test:
        name: "test 1.19.5"
        requires:
        - "build 1.19.5"
    - build:
        name: "build 1.19.4-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.4-alpine"
        nginx-tag: "1.19.4-alpine"
    - test:
        name: "test 1.19.4-alpine"
        requires:
        - "build 1.19.4-alpine"
    - build:
        name: "build 1.19.4"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.4"
        nginx-tag: "1.19.4"
    - test:
        name: "test 1.19.4"
        requires:
        - "build 1.19.4"
    - build:
        name: "build 1.19.3-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.3-alpine"
        nginx-tag: "1.19.3-alpine"
    - test:
        name: "test 1.19.3-alpine"
        requires:
        - "build 1.19.3-alpine"
    - build:
        name: "build 1.19.3"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.3"
        nginx-tag: "1.19.3"
    - test:
        name: "test 1.19.3"
        requires:
        - "build 1.19.3"
    - build:
        name: "build 1.19.2-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.2-alpine"
        nginx-tag: "1.19.2-alpine"
    - test:
        name: "test 1.19.2-alpine"
        requires:
        - "build 1.19.2-alpine"
    - build:
        name: "build 1.19.2"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.2"
        nginx-tag: "1.19.2"
    - test:
        name: "test 1.19.2"
        requires:
        - "build 1.19.2"
    - build:
        name: "build 1.19.1-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.1-alpine"
        nginx-tag: "1.19.1-alpine"
    - test:
        name: "test 1.19.1-alpine"
        requires:
        - "build 1.19.1-alpine"
    - build:
        name: "build 1.19.1"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.1"
        nginx-tag: "1.19.1"
    - test:
        name: "test 1.19.1"
        requires:
        - "build 1.19.1"
    - build:
        name: "build 1.19.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.0-alpine"
        nginx-tag: "1.19.0-alpine"
    - test:
        name: "test 1.19.0-alpine"
        requires:
        - "build 1.19.0-alpine"
    - build:
        name: "build 1.19.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.19.0"
        nginx-tag: "1.19.0"
    - test:
        name: "test 1.19.0"
        requires:
        - "build 1.19.0"
    - build:
        name: "build 1.18.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.18.0-alpine"
        nginx-tag: "1.18.0-alpine"
    - test:
        name: "test 1.18.0-alpine"
        requires:
        - "build 1.18.0-alpine"
    - build:
        name: "build 1.18.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.18.0"
        nginx-tag: "1.18.0"
    - test:
        name: "test 1.18.0"
        requires:
        - "build 1.18.0"
    - build:
        name: "build 1.17.10-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.10-alpine"
        nginx-tag: "1.17.10-alpine"
    - test:
        name: "test 1.17.10-alpine"
        requires:
        - "build 1.17.10-alpine"
    - build:
        name: "build 1.17.10"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.10"
        nginx-tag: "1.17.10"
    - test:
        name: "test 1.17.10"
        requires:
        - "build 1.17.10"
    - build:
        name: "build 1.17.9-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.9-alpine"
        nginx-tag: "1.17.9-alpine"
    - test:
        name: "test 1.17.9-alpine"
        requires:
        - "build 1.17.9-alpine"
    - build:
        name: "build 1.17.9"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.9"
        nginx-tag: "1.17.9"
    - test:
        name: "test 1.17.9"
        requires:
        - "build 1.17.9"
    - build:
        name: "build 1.17.8-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.8-alpine"
        nginx-tag: "1.17.8-alpine"
    - test:
        name: "test 1.17.8-alpine"
        requires:
        - "build 1.17.8-alpine"
    - build:
        name: "build 1.17.8"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.8"
        nginx-tag: "1.17.8"
    - test:
        name: "test 1.17.8"
        requires:
        - "build 1.17.8"
    - build:
        name: "build 1.17.7-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.7-alpine"
        nginx-tag: "1.17.7-alpine"
    - test:
        name: "test 1.17.7-alpine"
        requires:
        - "build 1.17.7-alpine"
    - build:
        name: "build 1.17.7"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.7"
        nginx-tag: "1.17.7"
    - test:
        name: "test 1.17.7"
        requires:
        - "build 1.17.7"
    - build:
        name: "build 1.17.6-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.6-alpine"
        nginx-tag: "1.17.6-alpine"
    - test:
        name: "test 1.17.6-alpine"
        requires:
        - "build 1.17.6-alpine"
    - build:
        name: "build 1.17.6"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.6"
        nginx-tag: "1.17.6"
    - test:
        name: "test 1.17.6"
        requires:
        - "build 1.17.6"
    - build:
        name: "build 1.17.5-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.5-alpine"
        nginx-tag: "1.17.5-alpine"
    - test:
        name: "test 1.17.5-alpine"
        requires:
        - "build 1.17.5-alpine"
    - build:
        name: "build 1.17.5"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.5"
        nginx-tag: "1.17.5"
    - test:
        name: "test 1.17.5"
        requires:
        - "build 1.17.5"
    - build:
        name: "build 1.17.4-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.4-alpine"
        nginx-tag: "1.17.4-alpine"
    - test:
        name: "test 1.17.4-alpine"
        requires:
        - "build 1.17.4-alpine"
    - build:
        name: "build 1.17.4"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.4"
        nginx-tag: "1.17.4"
    - test:
        name: "test 1.17.4"
        requires:
        - "build 1.17.4"
    - build:
        name: "build 1.17.3-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.3-alpine"
        nginx-tag: "1.17.3-alpine"
    - test:
        name: "test 1.17.3-alpine"
        requires:
        - "build 1.17.3-alpine"
    - build:
        name: "build 1.17.3"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.3"
        nginx-tag: "1.17.3"
    - test:
        name: "test 1.17.3"
        requires:
        - "build 1.17.3"
    - build:
        name: "build 1.17.2-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.2-alpine"
        nginx-tag: "1.17.2-alpine"
    - test:
        name: "test 1.17.2-alpine"
        requires:
        - "build 1.17.2-alpine"
    - build:
        name: "build 1.17.2"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.2"
        nginx-tag: "1.17.2"
    - test:
        name: "test 1.17.2"
        requires:
        - "build 1.17.2"
    - build:
        name: "build 1.17.1-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.1-alpine"
        nginx-tag: "1.17.1-alpine"
    - test:
        name: "test 1.17.1-alpine"
        requires:
        - "build 1.17.1-alpine"
    - build:
        name: "build 1.17.1"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.1"
        nginx-tag: "1.17.1"
    - test:
        name: "test 1.17.1"
        requires:
        - "build 1.17.1"
    - build:
        name: "build 1.17.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.0-alpine"
        nginx-tag: "1.17.0-alpine"
    - test:
        name: "test 1.17.0-alpine"
        requires:
        - "build 1.17.0-alpine"
    - build:
        name: "build 1.17.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.17.0"
        nginx-tag: "1.17.0"
    - test:
        name: "test 1.17.0"
        requires:
        - "build 1.17.0"
    - build:
        name: "build 1.16.1-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.16.1-alpine"
        nginx-tag: "1.16.1-alpine"
    - test:
        name: "test 1.16.1-alpine"
        requires:
        - "build 1.16.1-alpine"
    - build:
        name: "build 1.16.1"
        build-image: "datadog/docker-library:nginx-datadog-build-1.16.1"
        nginx-tag: "1.16.1"
    - test:
        name: "test 1.16.1"
        requires:
        - "build 1.16.1"
    - build:
        name: "build 1.16.0-alpine"
        build-image: "datadog/docker-library:nginx-datadog-build-1.16.0-alpine"
        nginx-tag: "1.16.0-alpine"
    - test:
        name: "test 1.16.0-alpine"
        requires:
        - "build 1.16.0-alpine"
    - build:
        name: "build 1.16.0"
        build-image: "datadog/docker-library:nginx-datadog-build-1.16.0"
        nginx-tag: "1.16.0"
    - test:
        name: "test 1.16.0"
        requires:
        - "build 1.16.0"
